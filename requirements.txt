# PyTorch BU DOSYADA YOK — ayrı index'ten kurulur.
# GPU gerekmez: tüm ölçümler tek iş parçacıklı CPU'da yapılır (bkz. README bölüm 4).
#   CPU  : pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA : pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128

# --- Çekirdek ---
numpy
scipy
pandas
matplotlib
tqdm
pyyaml

# --- Uydu görüntüsü G/Ç ---
rasterio          # GeoTIFF okuma (GDAL wheel içinde gelir, ayrıca kurma)
tifffile
pillow
opencv-python-headless   # albumentations bunu istiyor; GUI'li opencv-python ile birlikte kurma (cv2 cakisir)

# --- Model / metrik ---
timm              # MobileNetV3/V4 omurgaları
torchmetrics
scikit-learn      # precision / recall / F1 / confusion matrix
tabulate          # pandas .to_markdown() — make_report.py rapor tablolarını bununla üretir
python-docx       # md_to_docx.py — teknik raporun .docx sürümünü üretir
albumentations    # veri artırma

# --- Deney takibi ---
tensorboard

# --- Küçültme ve CPU çıkarım ölçümü ---
onnx
onnxscript        # torch>=2.9 onnx.export bunu istiyor
onnxruntime       # DİKKAT: onnxruntime-gpu KURMA, CPU süre ölçümünü bozar

# --- Demo ve ısı haritası ---
gradio
grad-cam          # import adı: pytorch_grad_cam

# --- Veri indirme ---
kaggle            # 95-Cloud veri seti
requests          # Zenodo (Sentinel-2 Cloud Mask Catalogue)
